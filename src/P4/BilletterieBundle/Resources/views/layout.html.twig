{# src/P4/BilletterieBundle/Resources/views/layout.html.twig #}

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">

    <style>
      /*@import url('https://fonts.googleapis.com/css?family=Open+Sans:400,700,800');*/
      @import url('https://fonts.googleapis.com/css?family=PT+Sans:400,700');
      @import url('https://fonts.googleapis.com/css?family=Cinzel:400,700');
    </style>

    <title>{% block title %}Billetterie{% endblock %}</title>

    {% block stylesheets %}
      {% stylesheets filter='cssrewrite, ?scssphp'
      'bundles/p4billetterie/css/bootstrap.v1.css'
       %}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
      {% endstylesheets %}
    {% endblock %}

    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css" rel="stylesheet">
  </head>

  <body>

  <header class="row col-lg-12 col-xs-12">
    <h1>Musée du Louvre</h1>
    <p>
      {{ 'site.devise'|trans }}
    </p>
  </header>

{#   <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Musée du Louvre</a>
      </div>

      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">

        {% if (app.request.getLocale()) == 'fr' %}
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ 'navbar.language'|trans }} <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
          <li class="dropdown-header"><strong>{{ 'navbar.choose'|trans }}</strong></li>
          <li class="active"><a href="#"> Fr</a></li>
          <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}"> En</a></li>
          </ul>
          </li>
        {% endif %}

        {% if (app.request.getLocale()) == 'en' %}
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ 'navbar.language'|trans }} <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
          <li class="dropdown-header"><strong>{{ 'navbar.choose'|trans }}</strong></li>
          <li class=""><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'fr'})) }}"> Fr</a></li>
          <li class="active"><a href=""> En</a></li>
          </ul>
          </li>
        {% endif %}

        </ul>
      </div><!--/.navbar-collapse -->
    </div>
  </nav> #}

{#   <nav class="navbar-default navbar-fixed-top">
    <div class="container">

      <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Musée du Louvre</a>
      </div>

      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
          {% if (app.request.getLocale()) == 'fr' %}
            <li class="dropdown">
              <li class="active"><a href="#"> Fr</a></li>
              <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}"> En</a></li>
            </li>
          {% endif %}
          {% if (app.request.getLocale()) == 'en' %}
            <li class="dropdown">
              <li class=""><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'fr'})) }}"> Fr</a></li>
              <li class="active"><a href=""> En</a></li>
            </li>
          {% endif %}
        </ul>
      </div>

    </div>
  </nav> #}

  <nav class="navbar-default navbar-fixed-top">
    <div class="container">

{#       <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
          </button>

          <a class="navbar-brand" href="#"><img src="" /> Musée du Louvre</a>
      </div> #}

      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
          {% if (app.request.getLocale()) == 'fr' %}
            <li class="dropdown">
              <li class="active"><a href="#"> Fr</a></li>
              <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}"> En</a></li>
            </li>
          {% endif %}
          {% if (app.request.getLocale()) == 'en' %}
            <li class="dropdown">
              <li class=""><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'fr'})) }}"> Fr</a></li>
              <li class="active"><a href=""> En</a></li>
            </li>
          {% endif %}
        </ul>
      </div>

    </div>
  </nav>

  <div class="row col-lg-12 col-xs-12">
  <div class="container">

    <div class="jumbotron col-lg-2 col-xs-12" id="infosPratiques">
      <div class="container-fluid col-lg-2 col-xs-12" id="containerInfosPratiques">
        <h4>{{ 'IP'|trans }} </h4>
        <h5>Adresse</h5>
        <p>Musée du Louvre, 75058 Paris - France</p>
        <h5>Téléphone</h5>
        <p>+ 33 (0)1 40 20 53 17</p>
        <h5>Horaires</h5>
        <p>Ouvert tous les jours de 9h à 18h sauf le mardi et dimanche.
        Nocturnes jusqu’à 21h45 le mercredi et le vendredi.
        </p>
        <h5>Tarifs</h5>
        <ul id="tarifs">
            <li><strong>16 € - tarif « normal »</strong> à partir de 12 ans</li>
            <li><strong>12 € - tarif « seniors »</strong> à partir de 60 ans</li>
            <li><strong>8 € - tarif « enfant »</strong> de 4 à 12 ans</li>
            <li><strong>10 € - tarif « réduit »</strong> accordé pour les étudiants, les employés du musée, ceux du Ministère de la Culture et les militaires. La présentation d'une carte attestant de ce statut sera demandé à l'entrée.
            </li>
            <li><strong>Gratuit</strong> pour les enfants de moins de 4 ans</li>
            <li><strong>Type de billet :</strong> Journée ou Demi-journée (09h-14h ou 14h-18h, tarif divisé par deux).</li>
        </ul>
      </div>
    </div>
    
    <div class="jumbotron col-lg-10 col-xs-12" id="formJumbo">
      <div class="container-fluid col-lg-10 col-xs-12" id="containerForm">

        {# BODY #}
        {% block body %}
        {% endblock %}

        <hr>

        {# FOOTER #}
        <footer>
          <p>Musée du Louvre © {{ 'now'|date('Y') }}</p>
        </footer>
      </div>
    </div>

  </div>
  </div>

  </body>
</html>